#pragma GCC optimize("O3,unroll-loops")
#include <bits/stdc++.h>
using namespace std;

// code by #CodeCrafters_Nholl (danglongnhat)

int main ()  {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    freopen("inp.txt", "r", std);
    freopen("out.txt", "w", std);

    long long t;    cin >> t;
    while (t--) { 
        long long a, b;
        cin >> a >> b;
        while 
    }

    return 0;
}